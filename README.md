# README

# create project
```bash
docker-compose run web rails new .  --database=mysql --skip-test --skip-turbolinks --force
```

# database.yml setting
database.ymlのhost修正

# create database
```bash
rails db:create
```
